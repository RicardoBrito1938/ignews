import styles from './style.module.scss'

export const SubscribeButton = () => {
    return (
        <button
            type="button"
            className={styles.subscribeButton}
        >
            Subscribe now
        </button>
    )
}